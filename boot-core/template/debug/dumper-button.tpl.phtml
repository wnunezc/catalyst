<button id="{{ btnId }}" class="{{ dumpId }}-btn" data-dumper-toggle="{{ modalId }}" title="Show Debug Information">
    <span class="{{ dumpId }}-btn-icon">&#128270;</span>
    <span class="{{ dumpId }}-btn-badge">{{ dumpCounter }}</span>
</button>
