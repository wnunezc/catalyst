<section class="admin-page-header card mb-3{{#if admin_header_modifier}} {{ admin_header_modifier }}{{/if}}" data-admin-page-header>
    <div class="card-body">
        <div class="admin-page-header__main">
            <div class="admin-page-header__copy">
                {{#if admin_header_eyebrow}}
                    <span class="admin-page-header__eyebrow">{{ admin_header_eyebrow }}</span>
                {{/if}}
                <h1 class="admin-page-header__title">{{ admin_header_title }}</h1>
                {{#if admin_header_description}}
                    <p class="admin-page-header__description">{{ admin_header_description }}</p>
                {{/if}}
            </div>

            {{#if admin_header_has_actions}}
                <div class="admin-page-header__actions" aria-label="{{ admin_header_actions_label }}">
                    {{#each admin_header_actions}}
                        {{#if is_submit}}
                            <button type="submit" class="{{ class }}"{{#if form}} form="{{ form }}"{{/if}}{{#if name}} name="{{ name }}"{{/if}}{{#if value}} value="{{ value }}"{{/if}}{{#if disabled}} disabled{{/if}}>
                                {{#if has_icon}}<i class="{{ icon }} me-1" aria-hidden="true"></i>{{/if}}{{ label }}
                            </button>
                        {{else}}
                            <button type="button" class="{{ class }}"{{#if href}} data-catalyst-href="{{ href }}"{{/if}}{{#if target}} data-catalyst-target="{{ target }}"{{/if}}{{#if modal_target}} data-bs-toggle="modal" data-bs-target="{{ modal_target }}"{{/if}}{{#if disabled}} disabled{{/if}}>
                                {{#if has_icon}}<i class="{{ icon }} me-1" aria-hidden="true"></i>{{/if}}{{ label }}
                            </button>
                        {{/if}}
                    {{/each}}
                </div>
            {{/if}}
        </div>

        {{#if admin_header_has_metrics}}
            <div class="admin-page-header__metrics" aria-label="{{ admin_header_metrics_label }}">
                {{#each admin_header_metrics}}
                    <div class="admin-page-header__metric{{#if tone_class}} {{ tone_class }}{{/if}}">
                        <span class="admin-page-header__metric-label">{{ label }}</span>
                        {{#if badge_class}}
                            <span class="badge {{ badge_class }} admin-page-header__metric-value">{{ value }}</span>
                        {{else}}
                            <span class="admin-page-header__metric-value{{#if value_class}} {{ value_class }}{{/if}}">{{ value }}</span>
                        {{/if}}
                    </div>
                {{/each}}
            </div>
        {{/if}}

        {{#if admin_header_has_tabs}}
            <nav class="admin-page-header__tabs" aria-label="{{ admin_header_tabs_label }}">
                {{#each admin_header_tabs}}
                    <button type="button" class="admin-page-header__tab{{#if is_active}} is-active{{/if}}" data-catalyst-href="{{ href }}"{{#if disabled}} disabled{{/if}}>
                        {{#if has_icon}}<i class="{{ icon }} me-1" aria-hidden="true"></i>{{/if}}{{ label }}
                    </button>
                {{/each}}
            </nav>
        {{/if}}
    </div>
</section>
