<!DOCTYPE html>
<html lang="{{ lang }}"
      dir="ltr"
      data-bs-theme="light"
      data-skin="default"
      data-layout-width="fluid"
      data-layout-position="fixed"
      data-menu-color="dark"
      data-sidenav-size="default"
      data-topbar-color="gray">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ document_title }}</title>

    {{#each meta_tags}}
    <meta name="{{ name }}" content="{{ content }}">
    {{/each}}

    <link rel="shortcut icon" href="/assets/vendor/inspinia/images/favicon.ico">
    <script type="application/json" id="catalyst-appearance-config" nonce="{{ csp_nonce }}">{{{ platform_appearance_json }}}</script>
    <script src="/assets/js/catalyst/appearance-bootstrap.js?v={{ appearance_bootstrap_asset_version }}" nonce="{{ csp_nonce }}"></script>
    <link href="/assets/vendor/inspinia/css/vendors.min.css" rel="stylesheet" type="text/css">
    <link href="/assets/vendor/inspinia/plugins/datatables/responsive.bootstrap5.min.css" rel="stylesheet" type="text/css">
    <link id="app-style" href="/assets/vendor/inspinia/css/app.min.css" rel="stylesheet" type="text/css">
    <link href="/assets/css/catalyst/red-cross-theme.css?v=20260601-sidenav-title-1" rel="stylesheet" type="text/css">
    <link href="/assets/css/catalyst/response-skins.css?v=20260601-sidenav-title-1" rel="stylesheet" type="text/css">
<link href="/assets/css/catalyst/inspinia-runtime-compat.css?v=20260530-runtime-compat-1" rel="stylesheet" type="text/css">
    <link href="/assets/css/catalyst/admin-surfaces.css?v=20260530-admin-surfaces-1" rel="stylesheet" type="text/css">

    {{#each style_links}}
    <link href="{{ href }}" rel="{{ rel }}"{{#if has_media}} media="{{ media }}"{{/if}}>
    {{/each}}
</head>
<body class="demo-ui-shell-body"
      data-admin-context="{{ active_admin_context }}"
      data-demoui-page="{{ demo_ui_page_slug }}"
      data-demoui-doc="{{ selected_doc_file }}">
<div class="wrapper">
    <header class="app-topbar">
        <div class="container-fluid topbar-menu">
            <div class="d-flex align-items-center gap-2">
                <div class="logo-topbar">
                    <a href="/dashboard" class="logo-light">
                        <span class="logo-lg"><img src="/assets/vendor/inspinia/images/logo.png" alt="logo"></span>
                        <span class="logo-sm"><img src="/assets/vendor/inspinia/images/logo-sm.png" alt="small logo"></span>
                    </a>
                    <a href="/dashboard" class="logo-dark">
                        <span class="logo-lg"><img src="/assets/vendor/inspinia/images/logo-black.png" alt="dark logo"></span>
                        <span class="logo-sm"><img src="/assets/vendor/inspinia/images/logo-sm.png" alt="small logo"></span>
                    </a>
                </div>


                <div id="search-box" class="app-search d-none d-xl-flex">
                    <input type="search" class="form-control topbar-search" name="search" placeholder="Search for something...">
                    <i class="ti ti-search app-search-icon text-muted"></i>
                </div>

            </div>

            <div class="d-flex align-items-center gap-2">
                <div class="topbar-item">
                    <button class="topbar-link" type="button"><i class="ti ti-building-community topbar-link-icon"></i></button>
                </div>
                <div id="simple-user-dropdown" class="topbar-item nav-user demo-ui-user-dropdown">
                    <div class="dropdown">
                        <a class="topbar-link dropdown-toggle drop-arrow-none px-2" data-bs-toggle="dropdown" role="button" tabindex="0" aria-haspopup="true" aria-expanded="false" aria-label="{{ auth_menu_label }}" title="{{ auth_menu_label }}">
                            <img src="{{ auth_avatar_src }}" width="32" class="rounded-circle me-lg-2 d-flex" alt="user-image">
                            <div class="d-lg-flex align-items-center gap-1 d-none">
                                <h5 class="my-0 demo-ui-account-label">{{ auth_menu_label }}</h5>
                                <i class="ti ti-chevron-down align-middle"></i>
                            </div>
                        </a>
                        <div class="dropdown-menu dropdown-menu-end">
                            <div class="dropdown-header demo-ui-account-summary">
                                <span class="demo-ui-account-summary__name">{{ auth_name }}</span>
                                {{#if has_auth_email}}
                                <span class="demo-ui-account-summary__email">{{ auth_email }}</span>
                                {{/if}}
                            </div>

                            <div class="dropdown-divider"></div>

                            <a href="#!" class="dropdown-item">
                                <i class="ti ti-user-circle me-1 fs-lg align-middle"></i>
                                <span class="align-middle">{{ t:ui.product_nav.profile }}</span>
                            </a>

                            <a href="#!" class="dropdown-item">
                                <i class="ti ti-settings-2 me-1 fs-lg align-middle"></i>
                                <span class="align-middle">{{ t:ui.product_nav.account_settings }}</span>
                            </a>

                            <div class="dropdown-divider"></div>

                            <a href="#!" class="dropdown-item">
                                <i class="ti ti-headset me-1 fs-lg align-middle"></i>
                                <span class="align-middle">{{ t:ui.product_nav.support }}</span>
                            </a>

                            <div class="dropdown-divider"></div>

                            <form method="POST" action="/logout" class="m-0" data-catalyst="form">
                                {{{ logout_csrf_field }}}
                                <button type="submit" class="dropdown-item text-danger fw-semibold">
                                    <i class="ti ti-logout me-1 fs-lg align-middle"></i>
                                    <span class="align-middle">{{ t:ui.status_bar.log_out }}</span>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <div class="sidenav-menu">
        <a href="/dashboard" class="logo">
                <span class="logo logo-light">
                    <span class="logo-lg"><img src="/assets/vendor/inspinia/images/logo.png" alt="logo"></span>
                    <span class="logo-sm"><img src="/assets/vendor/inspinia/images/logo-sm.png" alt="small logo"></span>
                </span>
                <span class="logo logo-dark">
                    <span class="logo-lg"><img src="/assets/vendor/inspinia/images/logo-black.png" alt="dark logo"></span>
                    <span class="logo-sm"><img src="/assets/vendor/inspinia/images/logo-sm.png" alt="small logo"></span>
                </span>
        </a>

        <div class="scrollbar" data-simplebar="" data-simplebar-auto-hide="false">
            <div id="sidenav-menu">
                <ul class="side-nav">
                    {{#each demo_ui_nav_groups}}
                    {{#if is_title}}
                    <li class="side-nav-title mt-2">{{ label }}</li>
                    {{else}}
                    {{#if is_link}}
                    <li class="side-nav-item {{#if is_active}}active{{/if}}">
                        <a href="{{ href }}" class="side-nav-link {{#if is_active}}active{{/if}}">
                            <span class="menu-icon"><i class="{{ icon }}"></i></span>
                            <span class="menu-text">{{ label }}</span>
                        </a>
                    </li>
                    {{else}}
                    <li class="side-nav-item {{#if is_active}}active{{/if}}">
                        <a data-shell-collapse="true"
                           href="#{{ collapse_id }}"
                           aria-expanded="{{ expanded }}"
                           aria-controls="{{ collapse_id }}"
                           class="side-nav-link {{#if is_active}}active{{/if}}">
                            <span class="menu-icon"><i class="{{ icon }}"></i></span>
                            <span class="menu-text">{{ label }}</span>
                            <span class="menu-arrow"></span>
                        </a>
                        <div class="collapse {{#if show}}show{{/if}}" id="{{ collapse_id }}">
                            <ul class="sub-menu">
                                {{#each items}}
                                <li class="side-nav-item {{#if is_active}}active{{/if}}">
                                    {{#if is_nested_collapse}}
                                    <a data-shell-collapse="true"
                                       href="#{{ collapse_id }}"
                                       aria-expanded="{{ expanded }}"
                                       aria-controls="{{ collapse_id }}"
                                       class="{{ link_class }}">
                                        <span class="menu-text">{{ label }}</span>
                                        {{#if badge_label}}
                                        <span class="{{ badge_class }}">{{ badge_label }}</span>
                                        {{/if}}
                                        <span class="menu-arrow"></span>
                                    </a>
                                    <div class="collapse {{#if show}}show{{/if}}" id="{{ collapse_id }}">
                                        <ul class="sub-menu">
                                            {{#each children}}
                                            <li class="side-nav-item {{#if is_active}}active{{/if}}">
                                                <a href="{{ href }}" class="{{ link_class }}">
                                                    <span class="menu-text">{{ label }}</span>
                                                </a>
                                            </li>
                                            {{/each}}
                                        </ul>
                                    </div>
                                    {{else}}
                                    <a href="{{ href }}" class="side-nav-link {{#if is_active}}active{{/if}}">
                                        <span class="menu-text">{{ label }}</span>
                                    </a>
                                    {{/if}}
                                </li>
                                {{/each}}
                            </ul>
                        </div>
                    </li>
                    {{/if}}
                    {{/if}}
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>

    <div class="content-page" data-simplebar="" data-simplebar-auto-hide="false">
        <div class="container-fluid">
            {{{ content }}}
        </div>
    </div>
</div>

{{> "../components/_theme-customizer-panel" }}

{{> "../components/_flash-messages" }}
{{> "../components/_toaster-container" }}
{{> "../components/_modal-container" }}
{{> "../components/_status-bar" }}
{{> "../components/_body-scripts" }}
{{> "../components/_catalyst-init" }}

{{#each script_links}}
<script src="{{ src }}"{{#if has_type}} type="{{ type }}"{{/if}}{{#if defer}} defer{{/if}}{{#if async}} async{{/if}}{{#if has_nonce}} nonce="{{ nonce }}"{{/if}}></script>
{{/each}}
</body>
</html>
