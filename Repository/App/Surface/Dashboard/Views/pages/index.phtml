<div data-account-page="dashboard">
    {{> "components._page-header" }}

    <section class="card">
        <div class="card-body py-5 text-center">
            <i class="ti ti-layout-dashboard fs-1 text-muted" aria-hidden="true"></i>
            <h2 class="h5 mt-3 mb-2">{{ t:dashboard.index.extension_title }}</h2>
            <p class="text-muted mb-0 mx-auto">{{ t:dashboard.index.extension_description }}</p>
        </div>
    </section>
</div>
