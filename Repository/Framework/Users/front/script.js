/**
 * Contract:
 * - Init: published with Users views as an intentional no-op placeholder.
 * - DOM: no selectors are consumed.
 * - Events/Payload: no listeners and no server payload.
 * - CSP: no inline handlers or parallel runtime initialization.
 */
