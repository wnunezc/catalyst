<div class="surface-page surface-page-shell">
    {{> "components._page-header" }}

    <section class="row g-3 mb-3" aria-label="{{ t:apimanagement.index.eyebrow }}">
        {{#each api_metrics}}
            <div class="col-12 col-sm-4">
                <article class="card h-100">
                    <div class="card-body">
                        <span class="small text-muted d-block mb-1">{{ label }}</span>
                        <strong class="d-block">{{ value }}</strong>
                    </div>
                </article>
            </div>
        {{/each}}
    </section>

    {{#if has_created_token}}
        <section class="card border-success shadow-sm mb-3">
            <div class="card-body">
                <div class="small text-uppercase fw-semibold text-success mb-2">{{ t:apimanagement.created_token.title }}</div>
                <p class="mb-2">{{ t:apimanagement.created_token.description }}</p>
                <pre class="apimanagement-token-secret mb-0">{{ created_token_plain_text }}</pre>
            </div>
        </section>
    {{/if}}

    <div class="row g-3 mb-3">
        <div class="col-xl-5">
            {{> "components._form-builder" }}
        </div>
        <div class="col-xl-7">
            <section class="card border-0 shadow-sm h-100">
                <div class="card-header">
                    <h2 class="h5 mb-1">{{ t:apimanagement.catalog.title }}</h2>
                    <p class="text-muted mb-0">{{ t:apimanagement.catalog.description }}</p>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover align-middle mb-0">
                            <thead class="thead-sm text-uppercase fs-xxs">
                            <tr>
                                <th>{{ t:apimanagement.catalog.columns.method }}</th>
                                <th>{{ t:apimanagement.catalog.columns.path }}</th>
                                <th>{{ t:apimanagement.catalog.columns.permission }}</th>
                                <th>{{ t:apimanagement.catalog.columns.description }}</th>
                            </tr>
                            </thead>
                            <tbody>
                            {{#each catalog_routes}}
                                <tr>
                                    <td><span class="badge text-bg-dark">{{ method }}</span></td>
                                    <td><code>{{ path }}</code></td>
                                    <td>{{ permission }}</td>
                                    <td>{{ description }}</td>
                                </tr>
                            {{/each}}
                            </tbody>
                        </table>
                    </div>
                </div>
            </section>
        </div>
    </div>

    <section class="card border-0 shadow-sm">
        <div class="card-header">
            <h2 class="h5 mb-1">{{ t:apimanagement.tokens.title }}</h2>
            <p class="text-muted mb-0">{{ t:apimanagement.tokens.description }}</p>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover align-middle mb-0">
                    <thead class="thead-sm text-uppercase fs-xxs">
                    <tr>
                        <th>{{ t:apimanagement.tokens.columns.name }}</th>
                        <th>{{ t:apimanagement.tokens.columns.prefix }}</th>
                        <th>{{ t:apimanagement.tokens.columns.user }}</th>
                        <th>{{ t:apimanagement.tokens.columns.abilities }}</th>
                        <th>{{ t:apimanagement.tokens.columns.expires }}</th>
                        <th>{{ t:apimanagement.tokens.columns.last_used }}</th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {{#each tokens_rows}}
                        <tr>
                            <td>{{ name }}</td>
                            <td><code>{{ token_prefix }}</code></td>
                            <td>#{{ user_id }}</td>
                            <td>{{ abilities_label }}</td>
                            <td>{{ expires_label }}</td>
                            <td>{{ last_used_label }}</td>
                            <td class="text-end">
                                <form data-catalyst="form" method="POST" action="{{ revoke_url }}" class="d-inline">
                                    {{{ @root.csrfField }}}
                                    <button type="submit" class="btn btn-sm btn-outline-danger">{{ t:apimanagement.tokens.actions.revoke }}</button>
                                </form>
                            </td>
                        </tr>
                    {{else}}
                        <tr><td colspan="7" class="text-muted">{{ t:apimanagement.tokens.empty }}</td></tr>
                    {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </section>
</div>
