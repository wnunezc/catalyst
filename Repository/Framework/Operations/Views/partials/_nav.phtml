<nav class="operations-tabs" aria-label="{{ aria_label }}">
    {{#each nav_items}}
        <button type="button" class="operations-tabs__item{{#if is_active}} is-active{{/if}}" data-catalyst-href="{{ href }}">
            {{ label }}
        </button>
    {{/each}}
</nav>
