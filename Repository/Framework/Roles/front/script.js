/**
 * Contract:
 * - Init: published with Roles admin views as an intentional no-op placeholder.
 * - DOM: no selectors are consumed today.
 * - Events/Payload: no listeners and no server payload.
 * - CSP: reserved for future externalized behavior; no inline handlers.
 */
